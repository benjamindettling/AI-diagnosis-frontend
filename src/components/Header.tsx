import React from "react";

const Header: React.FC = () => {
  return (
    <header className="diagnosis-header">
      <div className="container">
        <h2 className="section-title">AI Diagnosis Tool</h2>
      </div>
    </header>
  );
};

export default Header;
